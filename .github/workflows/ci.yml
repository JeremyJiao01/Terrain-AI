name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  CGB_WORKSPACE: /tmp/cgb-test

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.11", "3.12"]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          pip install -e ".[treesitter-full]"
          pip install pytest

      - name: Run layer dependency check
        run: python tools/dep_check.py

      - name: Run tests (unit tests — no LLM/external services required)
        run: |
          python -m pytest -v --tb=short \
            code_graph_builder/tests/foundation/ \
            --ignore=code_graph_builder/tests/foundation/test_call_resolution.py \
            code_graph_builder/tests/domains/core/test_graph_build.py \
            code_graph_builder/tests/domains/core/test_vector_store.py \
            code_graph_builder/tests/domains/upper/calltrace/ \
            code_graph_builder/tests/domains/upper/test_api_docs.py \
            code_graph_builder/tests/domains/upper/test_api_find.py \
            code_graph_builder/tests/domains/upper/test_client.py \
            code_graph_builder/tests/domains/upper/test_camel_agent.py \
            code_graph_builder/tests/domains/upper/test_config.py \
            code_graph_builder/tests/domains/upper/test_markdown_generator.py
